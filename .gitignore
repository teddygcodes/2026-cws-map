# macOS
.DS_Store

# Node (Next runtime + dev/CI tooling)
node_modules/
.validate-tmp.js

# Next.js build output
.next/
out/

# Env / secrets
.env
.env.local
.env*.local
.vercel

# Playwright MCP scratch output
.playwright-mcp/

# Claude runtime locks (keep launch.json, ignore transient state)
.claude/*.lock

# Editor / OS cruft
*.swp
*~
Thumbs.db
.env*
