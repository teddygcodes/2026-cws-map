# macOS
.DS_Store

# Node (dev/CI tooling only — the app ships no runtime deps)
node_modules/
.validate-tmp.js

# Playwright MCP scratch output
.playwright-mcp/

# Claude runtime locks (keep launch.json, ignore transient state)
.claude/*.lock

# Editor / OS cruft
*.swp
*~
Thumbs.db
